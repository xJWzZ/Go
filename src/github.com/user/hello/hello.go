package main

import (
	"fmt"

	"github.com/user/stringutil"
)

func main() {
	fmt.Println(stringutil.Reverse("!oG, olleH"))
	var x float64
	x = 20.0
	fmt.Println(x)
	fmt.Printf("x is of type %T\n", x)
	y := 42
	fmt.Println(y)
	fmt.Printf("y is of type %T\n", y)
	fmt.Println(y*2)

}

/* something added for testing github again*/
